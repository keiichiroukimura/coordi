<!DOCTYPE html>
<html>
  <head>
    <title>Coordi</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width">
  </head>
  
  <body>
    <header>
      <% if logged_in? %>
        <%= link_to "Logout", session_path(current_user.id), method: :delete, class: 'btn btn-default prof' %>
      <% else %>
        <%= link_to "Sign up", new_user_path, class: 'btn btn-default prof' %>
        <%= link_to "Login", new_session_path, class: 'btn btn-default prof'%>
      <% end %>
        <h2 style="margin-top: 50px; margin-left: 30px; color:white;">Coordi</h2>
        <p style="margin-left: 30px; color:white;">何着てどこいく？</p>
    <nav class="navbar navbar-expand-sm" style="background-color:#e3f2fd; margin-top: 30px">
      <ul class="navbar-nav">
        <li class="nav-item"><a href="../clothes/new.html" class="nav-link">投稿作成</a></li>
        <li class="nav-item", style="margin-left: 30px;"><a href="#" class="nav-link">マイページ</a></li>
        <li class="nav-item", style="margin-left: 30px;"><a href="#" class="nav-link">お気に入り</a></li>
    </nav>
  </div>
    </header>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
  <%= yield %>
  </body>
</html>
