<!DOCTYPE html>
<html>
  <head>
    <title>Coordi</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
    <meta name="viewport" content="width=device-width">
    
  </head>
  
  <body>
    <header>
      <% if logged_in? %>
        <%= link_to "Logout", session_path(current_user.id), method: :delete, class: 'btn btn-default prof',style: "color: white;" %>
      <% else %>
        <%= link_to "Sign up", new_user_path, class: 'btn btn-default prof', style: "color: white;" %>
        <%= link_to "Login", new_session_path, class: 'btn btn-default prof', style: "color: white;"%>
      <% end %>
        <h2 style="margin-top: 50px; margin-left: 30px; color: white;">Coordi</h2>
        <p style="margin-left: 30px; color: white;">何着てどこいく？</p>
    <nav class="navbar navbar-expand-sm" style="background-color: #F5FFFA; margin-top: 30px">
      <ul class="navbar-nav">
          <li class="nav-item", style="margin-left: 30px;"><%= link_to "投稿一覧", clothes_path, class:"nav-link" %></li>
        <% if logged_in? %>
          <li class="nav-item", style="margin-left: 30px;"><%= link_to "投稿作成", new_clothe_path, class:"nav-link" %></li>
          <li class="nav-item", style="margin-left: 30px;"><%= link_to "マイページ", user_path(current_user.id), class:"nav-link" %></li>
          <li class="nav-item", style="margin-left: 30px;"><%= link_to "いいね一覧", clothe_favorites_path(current_user.id), class:"nav-link" %></li>
        <% end %>
        
    </nav>
  </div>
    </header>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
  <%= yield %>
    <div id="back-to-top" style="position:fixed;right:25px;bottom:5px">
      <a href="#"><i class="far fa-hand-pointer fa-3x"></i></a>
    </div>
  </body>
</html>
